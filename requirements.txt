# ═══════════════════════════════════════════════════════════════════════
# Online Logistic Regression — Requirements
# ═══════════════════════════════════════════════════════════════════════
#
# This file pins exact versions for full reproducibility.
# Tested on Python 3.10+
#
# Install with:
#   pip install -r requirements.txt
#
# For development (with testing):
#   pip install -r requirements.txt
#   pip install pytest pytest-cov
# ═══════════════════════════════════════════════════════════════════════

# ── Core numerical computing ──────────────────────────────────────────
numpy==1.26.4
    # Fast vectorized operations, linear algebra
    # Used by: all core modules, data generation, evaluation

# ── Data manipulation and I/O ─────────────────────────────────────────
pandas==2.2.1
    # CSV reading, DataFrame operations
    # Used by: generate_sample_data.py, stream_loader.py, batch_vs_online.py

# ── Machine learning baseline ─────────────────────────────────────────
scikit-learn==1.4.1.post1
    # Batch LogisticRegression, StandardScaler, train_test_split, metrics
    # Used by: batch_vs_online.py (comparison baseline)
    # Also used by: generate_sample_data.py (make_classification for validation)

# ── Visualization ─────────────────────────────────────────────────────
matplotlib==3.8.3
    # Plotting comparison results
    # Used by: run_comparison.py

# ── Testing (optional, install separately if needed) ──────────────────
# pytest==8.1.1
#     # Unit testing framework
#     # Used by: tests/*
#
# pytest-cov==4.1.0
#     # Coverage reporting
#     # Used by: pytest --cov
#
# Install testing dependencies with:
#   pip install pytest==8.1.1 pytest-cov==4.1.0

# ═══════════════════════════════════════════════════════════════════════
# Version Notes
# ═══════════════════════════════════════════════════════════════════════
#
# numpy 1.26.4:
#   - Stable NumPy 1.x series
#   - Compatible with Python 3.10-3.12
#   - No breaking changes expected for this project
#
# pandas 2.2.1:
#   - Latest stable 2.x series
#   - Faster CSV reading with chunked iterators
#   - Backward compatible with 2.0+
#
# scikit-learn 1.4.1:
#   - Modern sklearn with consistent API
#   - LogisticRegression LBFGS solver is stable
#   - Metrics API unchanged since 1.0
#
# matplotlib 3.8.3:
#   - Current stable release
#   - GridSpec layout works reliably
#   - No known issues with this project's usage
#
# ═══════════════════════════════════════════════════════════════════════
# Compatibility
# ═══════════════════════════════════════════════════════════════════════
#
# Python version: 3.10, 3.11, 3.12
# OS: Linux, macOS, Windows
# Architecture: x86_64, arm64 (Apple Silicon)
#
# Known issues:
#   - None at this time
#
# ═══════════════════════════════════════════════════════════════════════
